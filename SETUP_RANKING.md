# 가치타자 — 공용(전 직원) 랭킹 붙이기

개인 랭킹은 설정 없이 바로 동작합니다. **여러 직원이 한 랭킹을 공유**하려면
아래처럼 Google 시트 + Apps Script 를 랭킹 서버로 씁니다. (YBTI 등록과 같은 방식, 무료)

소요 시간 약 10분. 프로그래밍 지식 불필요.

---

## 1단계 — 구글시트 만들기

1. [sheets.new](https://sheets.new) 로 새 시트 생성 → 이름을 `가치타자 랭킹` 등으로 저장
2. (시트 탭은 그대로 둬도 됩니다. 스크립트가 `scores` 탭을 자동 생성합니다.)

## 2단계 — Apps Script 코드 붙여넣기

1. 시트 상단 메뉴 **확장 프로그램 → Apps Script**
2. 열린 편집기의 기존 내용을 모두 지우고,
   이 폴더의 **`apps-script/Code.gs` 내용을 전부 복사해서 붙여넣기**
3. 💾 저장 (Ctrl+S)

## 3단계 — 웹 앱으로 배포

1. 편집기 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴(유형 선택) → **웹 앱**
3. 설정:
   - 설명: `가치타자 랭킹 v1`
   - **다음 사용자 인증 정보로 실행: 나(본인 계정)**
   - **액세스 권한이 있는 사용자: 모든 사용자**
     → 목록에 `모든 사용자` 와 `모든 사용자(익명 포함)` 둘 다 있으면,
       **로그인 없이도 되는 쪽**(보통 그냥 `모든 사용자`)을 고릅니다.
       직원이 구글 로그인 없이 점수를 등록할 수 있어야 하기 때문입니다.
4. **배포** → 권한 요청이 뜨면 본인 구글계정으로 허용
   (“이 앱은 Google에서 확인하지 않았습니다” → 고급 → *(안전하지 않음)으로 이동* → 허용)
5. 나오는 **웹 앱 URL** 복사 — `https://script.google.com/macros/s/XXXXXXXX/exec` 형태

## 3.5단계 — 잘 됐는지 즉석 확인

복사한 URL을 브라우저 주소창에 그대로 붙여 넣어 봅니다.
화면에 **`[]`** (빈 대괄호)만 나오면 정상입니다.
로그인 화면이나 오류가 나오면 3단계의 "액세스 권한"을 다시 확인하세요.

## 4단계 — 게임에 주소 넣기

`js/config.js` 파일에서 이 부분을 찾아 붙여넣습니다:

```js
STORAGE: {
  sheetApiUrl: 'https://script.google.com/macros/s/XXXXXXXX/exec',  // ← 여기에 3단계 URL
  autoSubmit: false,   // true 로 하면 게임 끝나자마자 자동 등록(버튼 없음)
  ...
}
```

저장하고 파일을 다시 업로드(배포)하면 끝입니다.

---

## 동작 방식

- 게임 종료 → 결과 화면에 **「🏆 전체 랭킹에 등록」** 버튼 표시
  (`autoSubmit: true` 면 버튼 없이 자동 등록)
- 등록하면 구글시트 `scores` 탭에 한 줄 추가됨
- 랭킹 화면(전체 / 오늘 / 부서별)은 시트 데이터를 읽어 계산
  - **1인 1최고기록**만 반영 (여러 번 등록해도 최고점만)
  - 동점 시: 점수 → 정확도 → 평균 응답시간 → 최고 콤보 순
- 공개 랭킹의 이름은 자동 마스킹(`정○훈`), 본인에게만 전체 이름 표시

## 자주 묻는 것

**Q. 코드를 고쳤는데 반영이 안 돼요**
→ Apps Script 에서 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**.
   새 배포를 또 만들면 URL 이 바뀌니, 기존 배포를 "새 버전"으로 갱신하세요.

**Q. 이름이 시트에 실명으로 저장되나요?**
→ 네(사내 행사 집계 목적). 화면 표시는 마스킹되지만 시트 원본은 실명입니다.
   민감하면 부서 대항전 종료 후 시트를 관리자만 접근하도록 두세요.

**Q. 아무나 점수를 조작해 등록할 수 있지 않나요?**
→ 웹 기반 게임의 한계입니다. `Code.gs` 가 말도 안 되는 점수(음수 과다·100만 초과)는
   거르지만, 정밀 방지는 안 됩니다. 사내 이벤트 수준에서는 충분하며,
   의심 기록은 시트에서 직접 삭제하면 됩니다.

**Q. 기록을 초기화하려면?**
→ 구글시트 `scores` 탭에서 데이터 행을 지우면 됩니다(헤더 1행은 남기기).
